const $=s=>document.querySelector(s);const nav=$('nav'),toggle=$('.menu-toggle');toggle.onclick=()=>{const open=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',open);nav.classList.toggle('open',open)};nav.onclick=e=>{if(e.target.closest('a')&&nav.classList.contains('open'))toggle.click()};document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav.classList.contains('open')){toggle.click();toggle.focus()}});for(const b of document.querySelectorAll('[data-filter]'))b.onclick=()=>{for(const t of document.querySelectorAll('[data-filter]'))t.setAttribute('aria-pressed',t===b);for(const item of document.querySelectorAll('[data-category]'))item.hidden=b.dataset.filter!=='All'&&item.dataset.category!==b.dataset.filter};for(const b of document.querySelectorAll('[data-select]'))b.onclick=()=>{$('#service').value=b.dataset.select;$('#booking').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'});$('#service').focus({preventScroll:true})};const date=$('#date');const today=new Date();date.min=[today.getFullYear(),String(today.getMonth()+1).padStart(2,'0'),String(today.getDate()).padStart(2,'0')].join('-');const form=$('form'),dialog=$('dialog');form.onsubmit=e=>{e.preventDefault();$('#name').setCustomValidity($('#name').value.trim()?'':'Please enter your name.');date.setCustomValidity(date.value<date.min?'Please choose today or a later date.':'');if(!form.reportValidity())return;const data=new FormData(form);$('#summary').textContent=[...data].map(([k,v])=>k[0].toUpperCase()+k.slice(1)+': '+v.trim()).join('\n');dialog.showModal()};$('#name').oninput=()=>$('#name').setCustomValidity('');date.oninput=()=>date.setCustomValidity('');$('#close').onclick=()=>dialog.close();$('#save').onclick=()=>{const u=URL.createObjectURL(new Blob(['DEMO REQUEST — NOT SENT\n\n'+$('#summary').textContent],{type:'text/plain'}));const a=document.createElement('a');a.href=u;a.download='demo-request.txt';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)};