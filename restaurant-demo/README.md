ZionFlow fictional premium demo. Static HTML, CSS and JavaScript, no paid runtime or form backend. Edit index.html for business copy, menu/services and hours; styles.css :root for colours; replace both hero WebP sizes and alt text. site.config.json is a reference manifest, not a runtime data source. All sample data must be replaced with approved public business details before a real client launch. Never copy private HubSpot notes. Request flow validates inputs and previews/downloads locally; it does not send or confirm a booking. Demo noindex is intentional. To enable a real booking use an approved provider and test delivery. Images created with built-in image generation; prompts documented in demo-source/ASSETS.md. Serve repository root with any static server; no install/build required for these two demos.
